/**
 * SearchPanel — Re-export of ProjectSearch for Zenith feature parity.
 * Punam's ProjectSearch.tsx + FindReplace.tsx provide the same functionality.
 */
export { default } from "./ProjectSearch";
